import { createTheme } from "@mui/material";
export const theme=createTheme({
 // Define your theme here (e.g., palette, typography, breakpoints, etc.)
  // Example:
  palette: {
    primary: {
      main: '#007bff',
    },
    secondary: {
      main: '#dc3545',
    },
  },
  // Add any other theme configurations you need
}) ;



export default theme;